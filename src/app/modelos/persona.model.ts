export class Persona {
    id?: number;
    nombre: string; apellido: string; comentario: string;
      edad: number; telefono: string; web: string;
    ciudad: string; email: string;  titulo: string;

/*     constructor( nombre: string, apellido: string,  comentario: string, edad: number, 
        telefono: string, web: string, ciudad: string, email: string, titulo: string ) {

        this.nombre = nombre; this.apellido= apellido; this.comentario = comentario; this.web = web;
        this.edad = edad; this.telefono = telefono; this.ciudad = ciudad;
         this.email = email; this.titulo = titulo;
    }  */
     
}
